# WebSense Environment Configuration
# Copy this file to .env for local overrides

# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development
ALLOWED_ORIGINS=*

# Optional LLM API Keys (Leave blank to use deterministic offline classifier)
GEMINI_API_KEY=
OPENAI_API_KEY=
